FROM node:20-slim
WORKDIR /app

# Installer git (requis par Baileys)
RUN apt-get update -qq && \
    apt-get install --no-install-recommends -y git && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

COPY package.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["node", "server.js"]
